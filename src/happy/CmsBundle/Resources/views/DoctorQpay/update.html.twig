{% extends '@happyCms/base.html.twig' %}
{% block pageheader %}Сувилагчийн QR засах хэсэг{% endblock %}

{% block body %}
    {% include ':inc:flashmsg.html.twig' %}

    {% if img %}
        <img src="/{{ localstatfolder }}{{ img }}" width="100">
    {% endif %}

    {{ form_start(edit_form) }}

    {{ form_widget(edit_form) }}
    <input class="btn btn-success" type="sbmit" value="Засах"/>
    {{ form_end(edit_form) }}
    <a style="float: left;margin-bottom: 10px" href="{{ path('cms_nurse_qpay_index', {'id': nid}) }}">Буцах</a>
{% endblock %}
